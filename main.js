define(["adioo/bind/repeater"], function(Repeater) {
    
    var Tab = {
        
        
    };
    
    /*
        config = {
            
            source: {
                
            },
            bind: [
                {
                    
                }
            ]
        }
    */
    
    return function(config) {
        
        var tab = Repeater(config);
        
        return tab;
    };
});