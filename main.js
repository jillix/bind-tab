define(["./list"], function(List) {
    
    var Tab = {
        
        
    };
    
    /*
        config = {
            
            source: {
                
            },
            bind: [
                {
                    
                }
            ]
        }
    */
    
    return function(config) {
        
        var tab = List(config);
        
        return tab;
    };
});